name: backend_runner
on: push

jobs:
  apply_kustomize:
    permissions:
      contents: 'read'
      id-token: 'write'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: testing actions
        run: ls -ltra

#      - id: 'auth'
#        uses: 'google-github-actions/auth@v3'
#        with:
#          project_id: ${{ vars.PROJECT_ID }}
#          workload_identity_provider: '${{ secrets.WORKLOAD_IDENTITY_FEDERATION }}'
#          service_account: ${{ vars.SERVICE_ACCOUNT }}@${{ vars.PROJECT_ID }}.iam.gserviceaccount.com


#      - name: Configure Docker for Artifact Registry
#        run: |
#          gcloud auth configure-docker us-central1-docker.pkg.dev --quiet
#
#      - name: Set up QEMU
#        uses: docker/setup-qemu-action@v3
#
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v3
#
#      - name: Login to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: manabpokhrel7
#          password: ${{ secrets.DOCKERHUB_TOKEN }}

#      - name: Build and push Python Image
#        uses: docker/build-push-action@v6
#        with:
#          no-cache: true
#          context: ./GCP
#          file: ./GCP/Dockerfile
#          push: true
#          tags: manabpokhrel7/python-cloud:${{ github.sha }}

      - name: Push the image tag to my infra repo helm chart value
        run: | 
          git clone https://github.com/manabpokhrel7/Kubeadm-cilium-Terraform.git
          cd Kubeadm-cilium-Terraform
          git init
          touch testfile
          ls -ltra
          git config --global user.email "cicdbotapp@example.com"
          git config --global user.name "manab pokhrel cicd app bot"
          git remote -v
          git add .
          git commit -m "Cicd change from application repo"
          git push origin main
          
          

#      - name: Build and push React Image
#        uses: docker/build-push-action@v6
#        with:
#          no-cache: true
#          context: ./my-app
#          file: ./my-app/Dockerfile
#          push: true
#          tags: us-central1-docker.pkg.dev/my-personal-terraform/python-repository/react-manab:v9

